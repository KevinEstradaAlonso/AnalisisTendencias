# === Secrets / credentials ===
# Firebase service account key (NEVER commit)
backend/src/PulsoCiudadano.Worker/firebase-credentials.json

# Common credential file patterns
**/*credentials*.json
**/*service-account*.json

# Env files (local secrets)
.env
.env.*
**/.env
**/.env.*

# === Node / Vite ===
node_modules/
dist/
.vite/

# === .NET ===
bin/
obj/

# Playwright (often ends up under bin/ during local runs)
.playwright/
**/.playwright/

# === OS / editor ===
.DS_Store
Thumbs.db
.vscode/
.vs/
.idea/
